import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Use src/ directory — Next.js detects it automatically
  // but we make it explicit for clarity
};

export default nextConfig;
